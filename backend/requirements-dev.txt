-r requirements.txt
pytest==8.3.3
httpx==0.27.2
# Một số test cố ý gọi API thật (Open-Meteo, NASA…) để kiểm chứng nguồn — chúng
# thi thoảng hụt do mạng/quá hạn mức, KHÔNG phải lỗi code. Tự chạy lại vài lần
# để CI xanh ổn định thay vì nhấp nháy đỏ vì hạ tầng bên ngoài.
pytest-rerunfailures==14.0
