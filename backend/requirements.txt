fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Database + xác thực
SQLAlchemy==2.0.36
PyJWT==2.9.0
bcrypt==4.2.0
email-validator==2.2.0

# Tính toán số: percentile/histogram cho dự báo xác suất (A3) và PSI phát hiện
# trôi (A14). Local + Render vốn có numpy qua onnxruntime, nhưng CI KHÔNG cài
# onnxruntime nên phải khai báo tường minh — nếu không CI đỏ ModuleNotFoundError.
numpy>=1.26,<3

# PostgreSQL cho production (SQLite là mặc định, không cần gì thêm):
# psycopg[binary]==3.2.3
